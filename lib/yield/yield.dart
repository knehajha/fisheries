import 'package:flutter/cupertino.dart';

class Yields extends StatefulWidget {
  const Yields({Key? key}) : super(key: key);

  @override
  State<Yields> createState() => _YieldsState();
}

class _YieldsState extends State<Yields> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
